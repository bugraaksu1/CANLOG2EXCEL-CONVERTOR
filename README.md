# CANLOG2EXCEL-CONVERTOR
Offline browser-based tool that converts CAN bus log files (.txt/.log/.asc) to Excel or CSV. Streams large files (150MB+) without crashing, parses timestamp, channel, frame ID, direction and data bytes into columns, and supports Frame ID filtering to shrink output. Runs fully client-side, no upload, single HTML file.
